# SimNurse — UX Issues Log

Generated by end-to-end user journey analysis (2026-03-06).
Last reconciled against live code: 2026-03-12 (code-verified audit).
Status legend: `open` | `fixed` | `ignored` | `deferred`

> **Reconciliation note (2026-03-12):** Seven issues previously marked `open` were confirmed **fixed in code** but had not been reflected in this log. Their statuses have been updated below with code evidence. Issues ISSUE-09, ISSUE-11, ISSUE-13, ISSUE-20, ISSUE-25, ISSUE-28, and the MiniMonitor RR omission (captured in ui-design-audit) are now correctly marked `fixed`.

---

## ISSUE-01 — No Landing / Onboarding Screen Before the Library
| Field | Detail |
|---|---|
| **Status** | fixed |
| **Severity** | Medium |
| **Business Impact** | High |
| **Phase** | Awareness |
| **Location** | `src/components/LibraryScreen.tsx` |
| **Description** | New learners land directly on a raw scenario list with no context about what SimNurse is or how it works. |
| **Expected** | A one-time splash or contextual heading explaining the app purpose, clinical level, and how to begin. |
| **Actual** | Bare scenario list labelled only "Simulations / Select a clinical case to begin." |
| **Reproduction** | Open the app URL cold (no localStorage data). First render is immediately the scenario list. |
| **Fix Applied** | Added a dismissible welcome banner (`#welcome-banner`) above the scenario list. The banner is shown on first visit (when `simnurse_welcome_dismissed` is absent from `localStorage`) and permanently hidden after the user taps "Got it, let's start" or the ✕ close button. The banner explains the app purpose, clinical level, and a 3-step "how to use" guide. |

---

## ISSUE-02 — All Scenarios Show Identical Metadata ("BLS / ACLS · Dynamic")
| Field | Detail |
|---|---|
| **Status** | fixed |
| **Severity** | Medium |
| **Business Impact** | Medium |
| **Phase** | Awareness |
| **Location** | `src/components/LibraryScreen.tsx` |
| **Description** | Every scenario card showed the same hardcoded "BLS / ACLS" and "Dynamic" badges. Learners could not triage case difficulty or clinical domain before selecting. |
| **Expected** | Per-scenario difficulty level, clinical focus (cardiac, respiratory, pediatric), or estimated sim duration. |
| **Actual** | Static, identical tags on all 15 scenarios. |
| **Reproduction** | Load the library. Observe all scenario cards. |
| **Fix Applied** | Added `ScenarioMeta` interface (`difficulty: 'Beginner'\|'Intermediate'\|'Advanced'`, `domain: ScenarioDomain`, `estimatedDurationSec: number`) and optional `meta?` field to `Scenario` type. Populated all 15 seed scenarios with accurate metadata. `LibraryScreen` now renders colour-coded difficulty badges (green/amber/red), domain badge, and "~N min" clock badge from `scenario.meta`. DB bumped to version 5 to reseed existing instances. |

---

## ISSUE-03 — `getScenarioIcon()` Does Not Cover All Scenario Titles
| Field | Detail |
|---|---|
| **Status** | fixed |
| **Severity** | Low |
| **Business Impact** | Low |
| **Phase** | Awareness |
| **Location** | `src/components/LibraryScreen.tsx:13-22` |
| **Description** | Scenarios like "Adult SVT", "Adult Unstable Bradycardia", "Adult PEA (Hypovolemia)" fell through to the generic `Stethoscope` fallback icon. |
| **Expected** | Distinct icons for bradycardia, SVT, PEA sub-types. |
| **Actual** | Generic `Stethoscope` for 5+ scenario types. |
| **Reproduction** | Load the library and observe icons for SVT, bradycardia, and PEA scenarios. |
| **Fix Applied** | Added four new keyword branches to `getScenarioIcon()` with matching Lucide icons: `bradycardia` → `Gauge`, `svt`/`vtach`/`tachycardia` → `Zap`, `pea` → `HeartOff`, `acs`/`stemi` → `Flame`. All 15 scenario titles now resolve to a distinct icon. |

---

## ISSUE-04 — Onboarding Tour Auto-Starts 1 Second After Scenario Start
| Field | Detail |
|---|---|
| **Status** | fixed |
| **Severity** | High |
| **Business Impact** | High |
| **Phase** | Acquisition / Activation |
| **Location** | `src/components/OnboardingTour.tsx:61-75` |
| **Description** | The tour fires via `setTimeout(..., 1000)` immediately after entering a scenario, blocking the Patient View and then forcibly switching to the Status tab at step 2. |
| **Expected** | Tour is opt-in only — triggered by the `#help-btn`. |
| **Actual** | Auto-fires 1 second after scenario start; tab-switching is implicit. |
| **Reproduction** | Clear `localStorage`. Start any scenario. Wait 1 second — tour modal appears. Click "Next" — active tab changes to Status. |
| **Fix Applied** | Removed the `setTimeout` auto-start block. Tour now only starts when `key` changes (i.e., help button clicked). The `scenarioActive` guard remains to prevent the tour from running on the library screen. |

---

## ISSUE-05 — `suppressedProcedures` Cleared on Every Scenario Start
| Field | Detail |
|---|---|
| **Status** | fixed |
| **Severity** | Low |
| **Business Impact** | Medium |
| **Phase** | Acquisition |
| **Location** | `src/App.tsx:186`, `src/components/ActionsScreen.tsx:433` |
| **Description** | `localStorage.removeItem('suppressedProcedures')` fires in `startScenarioRun()`, silently resetting the learner's "Don't show again" preferences on every new scenario. The "Reset Hidden Guides" button in `ActionsScreen` was also conditionally hidden when `suppressed` was empty. |
| **Expected** | Suppression preferences persist across runs. "Reset Hidden Guides" button is always accessible. |
| **Actual** | Preferences wiped silently on start; reset button hidden when nothing is suppressed. |
| **Reproduction** | Suppress a procedure guide. Return to library. Start any scenario — all guides reappear. |
| **Fix Applied** | Removed `localStorage.removeItem('suppressedProcedures')` from `startScenarioRun()`. Changed "Reset Hidden Guides" button to always render (not gated on `Object.keys(suppressed).length > 0`). |

---

## ISSUE-06 — No Scenario Preview or Confirmation Before Starting
| Field | Detail |
|---|---|
| **Status** | fixed |
| **Severity** | Low |
| **Business Impact** | Medium |
| **Phase** | Acquisition |
| **Location** | `src/components/LibraryScreen.tsx` |
| **Description** | Tapping a scenario card started the timed simulation immediately with no "Are you ready?" step. Learners could accidentally start the wrong case. |
| **Expected** | Brief scenario premise card with a "Begin Scenario" CTA before the engine starts ticking. |
| **Actual** | One click started the simulation. |
| **Reproduction** | Tap any scenario card in the library. |
| **Fix Applied** | Added `ScenarioPreviewModal` component in `LibraryScreen.tsx`. Card tap now sets `previewScenario` state which renders a bottom-sheet modal containing: patient name/age/gender, difficulty/domain/duration badges, presenting rhythm (with pulseless indicator), initial vitals grid (HR, SpO₂, BP, RR), and a "Begin Scenario" CTA. The simulation only starts when the learner taps "Begin Scenario". The modal dismisses on Cancel or backdrop tap. |

---

## ISSUE-07 — Onboarding Tour Forcibly Switches Active Tab
| Field | Detail |
|---|---|
| **Status** | fixed (resolved by ISSUE-04 fix) |
| **Severity** | High |
| **Business Impact** | High |
| **Phase** | Activation |
| **Location** | `src/components/OnboardingTour.tsx:86-91` |
| **Description** | Steps 2 and 3 of the tour call `setActiveTab('status')` automatically, teleporting the learner away from their current tab without warning. |
| **Expected** | Tab navigation during a tour step should be communicated, opt-in, or removed. |
| **Actual** | Silent tab switch on "Next". |
| **Fix Applied** | Resolved as part of ISSUE-04 fix — tour no longer auto-starts, so this side effect is suppressed. The `setActiveTab` call within the tour's step tracking remains for when the tour is explicitly triggered, but should be surfaced with a notice in a future iteration. |

---

## ISSUE-08 — `buildClinicalNote()` Generates Generic Text Post-Stabilisation
| Field | Detail |
|---|---|
| **Status** | fixed |
| **Severity** | Low |
| **Business Impact** | Low |
| **Phase** | Activation |
| **Location** | `src/components/PatientView.tsx:77-100` |
| **Description** | After successful intervention (e.g. atropine resolving bradycardia), the patient returns to normal vitals and the only narrative shown is "Patient is conscious and responsive." with no clinical context about the improvement. |
| **Expected** | Post-intervention narrative confirming improvement. |
| **Actual** | Generic single-line message. |
| **Reproduction** | Start "Adult Unstable Bradycardia". Apply oxygen + atropine successfully. Observe clinical note. |
| **Fix Applied** | Added optional `conclusion` field to `Scenario` type; all 25 seed scenarios given clinically specific conclusion text; `EvaluationSummary` renders `conclusion ?? 'Patient stabilized.'` on success outcome. |

---

## ISSUE-09 — Active Intervention Badges Overlap Patient Header on Small Viewports
| Field | Detail |
|---|---|
| **Status** | fixed |
| **Severity** | Medium |
| **Business Impact** | Medium |
| **Phase** | Activation |
| **Location** | `src/components/PatientView.tsx:206-221` |
| **Description** | Multiple active intervention badges (`absolute top-4 left-4 z-20`) stack vertically and overlap the "Patient View" heading and status label on 375 px viewports. |
| **Expected** | Badges should not cover the header area. |
| **Actual** | Absolute-positioned badges stack over the header. |
| **Reproduction** | Start "Adult VFib Arrest". Apply CPR, defibrillate, and epinephrine simultaneously. Observe on mobile. |
| **Fix Applied** | Badges moved to an in-flow `flex-wrap gap-2 mt-2` row inside `<header id="patient-view-header">`. Capped at `MAX_BADGES = 3` with a "+N more" overflow pill for additional active interventions. Absolute positioning removed entirely. |

---

## ISSUE-10 — Inconsistent Vital Gating: Free on Patient Tab, Locked on Status Tab
| Field | Detail |
|---|---|
| **Status** | fixed |
| **Severity** | Medium |
| **Business Impact** | Medium |
| **Phase** | Activation |
| **Location** | `src/components/PatientView.tsx:217-233` |
| **Description** | SpO2 and Rhythm are always visible on the Patient tab without any unlock step. On the Status tab, SpO2 VitalCard requires "Perform Inspection to unlock". This creates a confusing UX inconsistency. |
| **Expected** | Consistent gating rules, or an explicit design rationale surfaced to the learner. |
| **Actual** | Free on Patient, locked on Status. |
| **Reproduction** | Start any scenario. Compare SpO2 visibility on Patient tab vs Status tab. |
| **Fix Applied** | Floating SpO₂ and Rhythm badges on the Patient tab now gate their displayed values behind the `unlocked` prop. When vitals are not yet unlocked via inspection, both badges show `--` instead of the live value. Only the opacity dimming previously existed; the value gate is now enforced at `PatientView.tsx:256` and `PatientView.tsx:264`. |

---

## ISSUE-11 — Clinical Notes Container Clipped on Short Viewports
| Field | Detail |
|---|---|
| **Status** | fixed |
| **Severity** | Medium |
| **Business Impact** | Medium |
| **Phase** | Activation |
| **Location** | `src/components/PatientView.tsx:271-278` |
| **Description** | The clinical notes card uses `absolute bottom-24` and is pushed behind the fixed BottomNav + MiniMonitor on short screens (< 700 px tall), making the primary narrative invisible. |
| **Expected** | Notes always visible in the scrollable area. |
| **Actual** | Notes occluded by BottomNav on iPhone SE. |
| **Reproduction** | Set viewport to 375×667 px. Start any scenario. Observe bottom of Patient tab. |
| **Fix Applied** | Replaced `absolute bottom-24` positioning with an in-flow `div#clinical-notes-container` using `w-full px-4 pb-4` at the bottom of the `<section>`. Notes now scroll naturally with the page content and are not occluded by BottomNav. |

---

## ISSUE-12 — Actions Catalog Is Scenario-Agnostic (33 Actions Shown Always)
| Field | Detail |
|---|---|
| **Status** | ignored |
| **Severity** | High |
| **Business Impact** | High |
| **Phase** | Activation |
| **Location** | `src/components/ActionsScreen.tsx:23-343` |
| **Description** | All 33 actions are shown regardless of the active scenario. For "Adult Stroke (CVA)" only 5 actions are relevant; 28 are distractors, inflating cognitive load. |
| **Note** | Intentionally ignored per product decision. Full catalog remains as-is. |

---

## ISSUE-13 — Rejection Message Gives No Next-Step Hint
| Field | Detail |
|---|---|
| **Status** | fixed |
| **Severity** | High |
| **Business Impact** | High |
| **Phase** | Activation |
| **Location** | `src/hooks/useScenarioEngine.ts:416-435` |
| **Description** | When the learner applies an out-of-sequence action, the rejection message says "Incorrect sequence. This is not the appropriate next step in the protocol." with no hint about what the right next step is. |
| **Expected** | "The next expected action is: Defibrillate." |
| **Actual** | Generic rejection with no next-step disclosure. |
| **Reproduction** | Start "Adult VFib Arrest". Apply CPR before defibrillate. Observe modal message. |
| **Fix Applied** | `engineReducer` now computes `expectedId` (the next action in `expected_sequence`) and derives `expectedLabel` from `INTERVENTION_LABELS`. The rejection message appends `hintSuffix`: `" The next expected step is: ${expectedLabel}."` when an expected action exists. Example output: `"Protocol Deviation: Incorrect sequence. This is not the appropriate next step in the protocol. The next expected step is: Defibrillate (AED/Manual)."` |

---

## ISSUE-14 — ProcedureGuide Diagram Area Is a Permanent Placeholder
| Field | Detail |
|---|---|
| **Status** | deferred |
| **Severity** | Medium |
| **Business Impact** | Medium |
| **Phase** | Activation |
| **Location** | `src/components/ProcedureGuide.tsx:86-98` |
| **Description** | The 16:7 diagram area in ProcedureGuide permanently shows "Procedural Diagram" with a pulsing circle. No actual diagram content is provided for any action. |
| **Note** | Deferred — to be addressed in a future iteration when procedural diagrams are available. |

---

## ISSUE-15 — Procedure Suppression Is Not Namespaced Per Scenario
| Field | Detail |
|---|---|
| **Status** | fixed |
| **Severity** | Low |
| **Business Impact** | Medium |
| **Phase** | Activation |
| **Location** | `src/components/ProcedureGuide.tsx:33-35`, `src/components/ActionsScreen.tsx:345-355` |
| **Description** | `suppressedProcedures[action.id]` uses the same key across all scenarios. Suppressing CPR guide in Scenario 1 suppresses it in Scenario 15 (Pediatric), where protocol steps differ. |
| **Expected** | Suppression keyed per `scenario_id + action.id`. |
| **Actual** | Global suppression key. |
| **Fix Applied** | Added `disabled` prop to `ActionsScreen`; when scenario `status !== 'running'`, actions list is wrapped in `pointer-events-none opacity-50` with a 'Scenario complete — no further actions available' banner. |

---

## ISSUE-16 — ECGWaveform Disconnected from Patient State
| Field | Detail |
|---|---|
| **Status** | fixed |
| **Severity** | High |
| **Business Impact** | High |
| **Phase** | Activation |
| **Location** | `src/components/ECGWaveform.tsx:1-105` |
| **Description** | The ECG waveform always shows a normal sinus pattern regardless of the patient's actual rhythm (VFib, VTach, Asystole, PEA, SVT, Bradycardia). The component accepted no props. |
| **Expected** | ECG morphology changes to match `PatientState.rhythm` and `pulsePresent`. |
| **Actual** | Hardcoded sinus P-QRS-T for all rhythms. |
| **Reproduction** | Start "Adult VFib Arrest" (rhythm: VFib). Navigate to Status tab. ECG shows normal sinus. |
| **Fix Applied** | Added `rhythm: HeartRhythm` and `pulsePresent: boolean` props to `ECGWaveform`. Implemented distinct canvas rendering paths: VFib (chaotic irregular), VTach (fast wide monomorphic), Asystole (flat line), PEA (organised but pulseless — rendered as a low-amplitude slow-rate pattern), SVT (fast narrow regular), Bradycardia (slow narrow regular), Sinus (normal). Label updated to show rhythm name. |

---

## ISSUE-17 — Vital Unlock Mechanic Has No Instructional Context
| Field | Detail |
|---|---|
| **Status** | open |
| **Severity** | Medium |
| **Business Impact** | Medium |
| **Phase** | Activation |
| **Location** | `src/components/VitalCard.tsx:69-75`, `src/components/StatusDashboard.tsx:28` |
| **Description** | New learners see `--` values and a tiny text link "Perform Inspection to unlock" with no explanation of the mechanic. |
| **Expected** | A brief inline explanation of the unlock mechanic on first view, with an accessible touch target. |
| **Partial Fix Applied** | `VitalCard` "Perform Inspection to unlock" button now has `min-h-[44px] flex items-center` meeting 44 px touch target (was visually 10px-tall link only). `StatusDashboard` adds a dismissible instructional banner gated on `simnurse_inspection_hint_dismissed` in localStorage. Text size increased to 12px (`text-xs`). |
| **Remaining gap** | The hint banner is on the Status tab — learners on the Patient tab see SpO₂ freely (ISSUE-10) and may never discover the unlock mechanic through normal flow. The banner contrast (`text-medical-700` on white ≈ 4.2:1) still falls below WCAG AA for body text. |

---

## ISSUE-18 — Patient Demographics Hardcoded as "John Doe • 45yo M"
| Field | Detail |
|---|---|
| **Status** | fixed |
| **Severity** | Low |
| **Business Impact** | Medium |
| **Phase** | Activation |
| **Location** | `src/components/StatusDashboard.tsx:48` |
| **Description** | The Status tab header always shows "Patient: John Doe • 45yo M" regardless of the active scenario. For pediatric or pregnant scenarios this is clinically contradictory. |
| **Expected** | Patient demographics derived from the active scenario. |
| **Actual** | Hardcoded static string. |
| **Reproduction** | Start "Pediatric Resp. Arrest (Asthma)". Navigate to Status tab. Observe "John Doe • 45yo M". |
| **Fix Applied** | Added `patient` optional field to the `Scenario` type (`name`, `age`, `gender`). Populated all 15 seed scenarios with appropriate demographics. `StatusDashboard` now receives `patient` as a prop and renders the live values. Fallback to "Unknown Patient" if not provided. |

---

## ISSUE-19 — Scenario Progress Bar Stays Flat at 0% for Multiple Minutes
| Field | Detail |
|---|---|
| **Status** | open |
| **Severity** | Medium |
| **Business Impact** | Medium |
| **Phase** | Retention |
| **Location** | `src/lib/scenarioProgress.ts:179-180` |
| **Description** | For arrest scenarios with no baseline progressions, the progress bar stays at 0% until the first protocol step is applied. A flat bar communicates "nothing is happening". |
| **Expected** | Time elapsed or preparatory steps contribute a small progress signal. |
| **Partial Fix Applied** | `calculateScenarioProgress()` now includes `elapsedContribution = Math.min(10, Math.floor(elapsedSec / 10) * 0.5)` — a maximum of 10% time-based ambient contribution at 0.5% per 10 seconds. This raises the floor from exactly 0% after ~40 seconds. |
| **Remaining gap** | For arrest scenarios, the bar still reads 0% for the first ~20–40 seconds before the elapsed contribution becomes perceptible. The 10% cap means the bar still communicates minimal progress before the first protocol step fires. A more visible sub-indicator for "time elapsed" remains a recommended improvement. |

---

## ISSUE-20 — Dual Sticky Bars (Header + MiniMonitor) Consume 112 px
| Field | Detail |
|---|---|
| **Status** | fixed |
| **Severity** | Medium |
| **Business Impact** | Medium |
| **Phase** | Retention |
| **Location** | `src/components/Header.tsx:89-243` |
| **Description** | `Header` (sticky top-0 z-50) and `MiniMonitor` (sticky top-0 z-40) both stick at the top simultaneously, consuming ~112 px before any content area begins. On iPhone SE this leaves only ~555 px for scrollable content. |
| **Expected** | A single composite sticky area ≤ 60 px, or a collapsible MiniMonitor. |
| **Actual** | Two independent sticky bars. |
| **Fix Applied** | `MiniMonitor` was merged into `Header` as a second row (`id="mini-monitor"`) rendered conditionally when `monitorState !== null`. The combined header is `sticky top-0 z-50` only — no second sticky element. The vital strip now shows all 4 vitals (HR, BP, SpO₂, RR) with urgency tier coloring (`critical`/`warning`/`normal`) and vital decay arrows (↑↓). A third conditional row (`id="urgency-strip"`) shows failure proximity and intervention countdown pills. The standalone `MiniMonitor.tsx` component is no longer mounted in `App.tsx`. |

---

## ISSUE-21 — Engine Interval Fires One Extra Tick After Status Change
| Field | Detail |
|---|---|
| **Status** | open |
| **Severity** | Medium |
| **Business Impact** | Low |
| **Phase** | Retention |
| **Location** | `src/hooks/useScenarioEngine.ts:666-676` |
| **Description** | Up to one additional `tick` dispatch fires after `status` transitions to `'failed'` or `'success'` before the `useEffect` cleanup clears the interval. The reducer guard prevents state mutation but the dispatch still occurs. |
| **Expected** | Zero interval calls after status transition. |
| **Actual** | Up to one extra tick. |
| **Recommendation** | Add an early guard inside the `setInterval` callback: `if (state.status !== 'running') return;`. |

---

## ISSUE-22 — Win/Loss Evaluated Against `displayState`, Not `baseState`
| Field | Detail |
|---|---|
| **Status** | fixed |
| **Severity** | High |
| **Business Impact** | High |
| **Phase** | Retention |
| **Location** | `src/hooks/useScenarioEngine.ts:576-583` |
| **Description** | Success and failure conditions are evaluated against `displayState` (base + active overrides). A `state_override` from an active intervention could prematurely satisfy a success condition even though the base physiological state has not genuinely improved. |
| **Expected** | Conditions evaluated against `baseState`. |
| **Actual** | Evaluated against `displayState`. |
| **Fix Applied** | `updateFailureHolds` and `updateSuccessHolds` in `engineReducer` now receive `nextBaseState` instead of `nextDisplayState`. Win/loss conditions are now evaluated against the raw physiological state, preventing intervention `state_override` values from prematurely satisfying success/failure conditions. Changed at `useScenarioEngine.ts` tick branch. |

---

## ISSUE-23 — `success_chance: 1` Still Calls `Math.random()`
| Field | Detail |
|---|---|
| **Status** | fixed |
| **Severity** | Low |
| **Business Impact** | Low |
| **Phase** | Retention |
| **Location** | `src/hooks/useScenarioEngine.ts:453-454` |
| **Description** | Guaranteed-success interventions (`success_chance: 1`) still invoke `Math.random()`. While `roll <= 1.0` is always true, this is semantically unclear and fragile if `success_chance` ever exceeds 1.0 by accident. |
| **Expected** | Guaranteed-success interventions bypass the roll entirely. |
| **Actual** | Roll still computed. |
| **Fix Applied** | The success condition inside `apply_intervention` branch changed from `if (roll <= definition.success_chance)` to `if (definition.success_chance >= 1 || roll <= definition.success_chance)`. Guaranteed-success interventions (`success_chance >= 1`) now bypass the random roll comparison entirely. |

---

## ISSUE-24 — Score Unfairly Penalises Accidental Duplicate Actions
| Field | Detail |
|---|---|
| **Status** | fixed |
| **Severity** | High |
| **Business Impact** | High |
| **Phase** | Retention |
| **Location** | `src/App.tsx:250-257` |
| **Description** | Score = `correctActions / evalActions.length * 100`. A learner who applies 3 correct actions + 1 accidental duplicate (rejected) scores 75% instead of 100%, even though their clinical decision-making was sound. |
| **Expected** | Score distinguishes between sequence errors (wrong action) and execution accidents (duplicate). |
| **Actual** | Both reduce score equally. |
| **Fix Applied** | `buildActionFeedback()` in `App.tsx` now detects duplicate/cooldown rejections (messages starting with "Already") and sets `isDuplicate: true` on those `ActionFeedback` entries. The `score` useMemo now uses `correctActions / (correctActions + sequenceErrors)` — duplicates are excluded from the denominator. In `EvaluationSummary`, duplicate actions display with amber styling (`bg-amber-50/50`, `AlertTriangle` icon) instead of red, and the "Review Protocol" link is suppressed for them. |

---

## ISSUE-25 — "Review Protocol" in Debrief Destroys the Debrief View
| Field | Detail |
|---|---|
| **Status** | fixed |
| **Severity** | Medium |
| **Business Impact** | Medium |
| **Phase** | Retention |
| **Location** | `src/components/EvaluationSummary.tsx:151,217-229` |
| **Description** | Clicking "Review Protocol" from the debrief timeline calls `setShowSummary(false)`, navigates to the Actions tab, and opens the ProcedureGuide. After closing the guide, the learner is left in the scenario Actions tab with no way back to the debrief. |
| **Expected** | Protocol guide opens as an overlay on top of the debrief. |
| **Actual** | Debrief is destroyed when "Review Protocol" is clicked. |
| **Fix Applied** | `EvaluationSummary` now manages a local `reviewAction` state. The Timeline's "Review Protocol" button calls `setReviewAction(ACTIONS.find(a => a.id === id))` which renders `<ProcedureGuide>` as a portal overlay directly on top of the debrief — `showSummary` is never modified. `onReviewProcedure` in `App.tsx` is now a documented no-op (`"Review is now handled inside EvaluationSummary via ProcedureGuide portal"`). A `useEffect` cleanup in `EvaluationSummary` clears `reviewAction` on unmount. |

---

## ISSUE-26 — "Try Again" Restarts From In-Memory Scenario Clone
| Field | Detail |
|---|---|
| **Status** | fixed |
| **Severity** | Medium |
| **Business Impact** | Medium |
| **Phase** | Retention |
| **Location** | `src/App.tsx:317` |
| **Description** | `onRestart={() => startScenarioRun(activeScenario)}` passes the in-memory clone from the previous run rather than fetching a fresh definition from the Dexie DB. Currently safe because the engine doesn't mutate the scenario object, but architecturally fragile. |
| **Expected** | Each restart sources the scenario from the Dexie DB canonical seed. |
| **Actual** | Restarts from previous run's clone. |
| **Fix Applied** | Restart handler in `App.tsx` now re-fetches the scenario from Dexie by `scenario_id` before calling `startScenarioRun`; falls back to in-memory copy if DB unavailable. |

---

## ISSUE-27 — No Session History or Cross-Session Progress Tracking
| Field | Detail |
|---|---|
| **Status** | fixed |
| **Severity** | Medium |
| **Business Impact** | High (institutional) |
| **Phase** | Advocacy |
| **Location** | `src/lib/db.ts` |
| **Description** | `sessionLogs` stores all run data in IndexedDB but nothing in the UI reads historical records. Learners cannot compare scores across runs or track improvement over time. |
| **Expected** | Historical score display on library screen (last 3 runs per scenario). |
| **Actual** | Session log data is write-only from the UI's perspective. |
| **Fix Applied** | `LibraryScreen` now uses Dexie's `useLiveQuery` to fetch the last 3 completed runs per scenario from `db.sessionLogs`. Each scenario card displays a dot-based history strip below the meta badges: green dot (`bg-emerald-500`) for `'success'` outcomes, red dot (`bg-red-500`) for `'failed'` outcomes, with `title="Run N: outcome"` tooltips. The strip is not rendered when a scenario has no history. |

---

## ISSUE-28 — `IncorrectActionWidget` Not Portal-Rendered on Wide Viewports
| Field | Detail |
|---|---|
| **Status** | fixed |
| **Severity** | Low |
| **Business Impact** | Low |
| **Phase** | Activation |
| **Location** | `src/components/IncorrectActionWidget.tsx:23-66` |
| **Description** | The widget uses `fixed inset-0` inside `#app-shell` (max-w-440px). On desktop, the backdrop covers the full viewport but the modal is centred in the 440 px column. Semantically the element escapes its parent. |
| **Expected** | Portal-rendered or scoped overlay matching the app shell width. |
| **Actual** | `fixed inset-0` inside a constrained container. |
| **Fix Applied** | `IncorrectActionWidget` now uses `createPortal(…, document.body)`. The backdrop is scoped with `left-1/2 -translate-x-1/2 w-full max-w-[440px] inset-y-0` to constrain it to the 440 px app column on wide viewports. Added `role="dialog" aria-modal="true" aria-labelledby="incorrect-action-title"` on the modal container and `id="incorrect-action-title"` on the `<h2>`. Focus is moved to `#incorrect-action-acknowledge-btn` via `useEffect` on `message` change. CSS shake animation class added to modal for stronger error communication. |

---

## ISSUE-29 — OnboardingTour Overlay Uses Complex `clip-path` Polygon
| Field | Detail |
|---|---|
| **Status** | fixed |
| **Severity** | Medium |
| **Business Impact** | Medium |
| **Phase** | Acquisition |
| **Location** | `src/components/OnboardingTour.tsx:200-202` |
| **Description** | The tour spotlight uses a 10-point CSS `clip-path` polygon on a full-overlay div. Complex `clip-path` on large elements has known rendering issues in Safari iOS, potentially breaking the spotlight effect. |
| **Expected** | Consistent spotlight across all browsers. |
| **Actual** | Potentially broken clipping on Safari. |
| **Fix Applied** | The single `clip-path` polygon overlay div was replaced with four semi-transparent panel divs (top, right, bottom, left) surrounding the target element. Panel background updated to Tailwind classes `bg-slate-900/60 backdrop-blur-sm` (consistent with the rest of the overlay system). Z-index corrected to `999` to match the parent wrapper. No `clip-path` CSS remains — the implementation is now Safari iOS-safe. |
